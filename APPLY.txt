Kathanika V50 SEO verifier hotfix

Replace:
  scripts/verify-pages-build.mjs

Then run:
  npm run verify:pages
  npm run preflight:pages

This fixes the title-validation regex only. It does not change site design, content, routes, or SEO metadata.
