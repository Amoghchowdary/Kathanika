KATHANIKA MEDIA V51 — GOOGLE INDEXING / CUSTOM DOMAIN

Production domain:
https://www.kathanika.in/

Submit this sitemap in Google Search Console:
https://www.kathanika.in/sitemap.xml

Recommended handoff:
1. Confirm https://www.kathanika.in/ loads after GitHub finishes TLS certificate issuance.
2. In GitHub Pages, enable Enforce HTTPS as soon as the checkbox becomes available.
3. Add/verify `https://www.kathanika.in/` (or the domain property `kathanika.in`) in Google Search Console.
4. Submit `https://www.kathanika.in/sitemap.xml`.
5. Use URL Inspection for Home, About, Work, Services, Career Inquiry and Business Inquiry.
6. Request indexing for priority pages after the live test succeeds.
7. Monitor Page Indexing, Core Web Vitals and HTTPS reports.

Technical SEO in V51:
- Root-domain canonical URLs
- Root base path (`/`)
- Organization + WebSite JSON-LD using www.kathanika.in
- Open Graph/Twitter URLs using www.kathanika.in
- XML sitemap with image discovery entries
- robots.txt advertising the custom-domain sitemap
- CNAME for www.kathanika.in
- No generated canonical dependency on amoghchowdary.github.io/Kathanika/

`sitemap.yml` remains a human-readable manifest. Google uses `sitemap.xml`.
